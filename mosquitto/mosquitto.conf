# Minimal Mosquitto config for local testing
# Listen on all interfaces, default MQTT port
listener 1883 0.0.0.0
protocol mqtt

# Allow anonymous connections for testing only
allow_anonymous true

# Persistence optional
persistence true
persistence_location /mosquitto/data/

log_timestamp true
log_type all
